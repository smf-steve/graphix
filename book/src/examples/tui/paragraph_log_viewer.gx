use tui;
use tui::paragraph;
use tui::text;

let log_entries = [];
let new_entry: string = sys::net::subscribe("/local/logs/application")?;

log_entries <- array::window(
    #n: 100,
    new_entry ~ log_entries,
    line(new_entry)
);

paragraph(&log_entries)
